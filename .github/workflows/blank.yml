name: Unit Testing

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Unit Tests
      run:
        npm install 
        npm run test:unit
